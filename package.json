{
  "name": "mars-rover-mission-control",
  "version": "1.0.0",
  "description": "Interactive NASA Mars Rover Data Visualization - Professional mission control interface",
  "private": true,
  "dependencies": {
    "axios": "^1.12.2",
    "chart.js": "^4.5.0",
    "date-fns": "^4.1.0",
    "framer-motion": "^12.23.13",
    "leaflet": "^1.9.4",
    "proj4": "^2.19.10",
    "proj4leaflet": "^1.0.2",
    "react": "18.2.0",
    "react-chartjs-2": "^5.3.0",
    "react-dom": "18.2.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "devDependencies": {
    "serve": "^14.2.5"
  },
  "scripts": {
    "start": "HOST=0.0.0.0 PORT=5000 BROWSER=none react-scripts start",
    "dev": "npm start",
    "build": "react-scripts build",
    "build:analyze": "npm run build && npx serve -s build -p 3001",
    "test": "react-scripts test",
    "test:coverage": "react-scripts test --coverage --watchAll=false",
    "test:ci": "CI=true react-scripts test --coverage --watchAll=false",
    "eject": "react-scripts eject",
    "serve": "serve -s build -p 5000",
    "lint": "echo 'No linting configured - add ESLint for production use'",
    "format": "echo 'No formatting configured - add Prettier for production use'",
    "setup": "npm install && echo '✅ Mars Rover Mission Control setup complete! Run npm start to launch.'",
    "health-check": "curl -f http://localhost:5000 || echo 'Server not running. Run npm start first.'",
    "clean": "rm -rf build node_modules package-lock.json && npm install",
    "deploy:build": "GENERATE_SOURCEMAP=false npm run build",
    "deploy:preview": "npm run deploy:build && vercel",
    "deploy:production": "npm run deploy:build && vercel --prod",
    "vercel-build": "npm run build",
    "postbuild": "echo '🚀 Build complete! Ready for deployment.'"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "keywords": [
    "mars",
    "nasa",
    "rover",
    "perseverance",
    "mission-control",
    "data-visualization",
    "react",
    "space",
    "astronomy"
  ],
  "author": "Mars Mission Control Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/mars-rover-mission-control.git"
  },
  "homepage": "https://your-mars-rover-app.vercel.app",
  "engines": {
    "node": ">=16.0.0"
  }
}
